{
  "phases": {
    "setup": {
      "nixPkgs": ["python313"]
    },
    "install": {
      "cmds": [
        "python -m venv /app/.venv",
        ". /app/.venv/bin/activate && pip install -r requirements.txt"
      ]
    },
    "build": {
      "cmds": [
        ". /app/.venv/bin/activate && python manage.py collectstatic --no-input",
        "chmod +x start.sh"
      ]
    }
  },
  "start": {
    "cmd": "bash start.sh"
  }
}
